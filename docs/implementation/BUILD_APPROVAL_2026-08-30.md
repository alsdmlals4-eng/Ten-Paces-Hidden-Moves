# Public Approach-Pressure Policy Recovery Build Approval

- Gate: `TEN-DEC-20260830-BALANCE-INSTRUMENTATION-CONTRACT-01`
- Parent decision: `TEN-DEC-20260830-BALANCE-INSTRUMENTATION-CONTRACT-01`
- Approved on: `2026-08-30 KST`
- Approval source: user instruction `승인` and continuing in-scope recovery authorization
- Pull request: `#283`
- Authority level: `VALIDATION_ONLY_POLICY_SEMANTIC_RECOVERY`

## Approved scope

Restore the already-approved public `public_approach_pressure` policy meaning: when no legal, affordable public attack reaches the opponent and the distance is greater than zero, select the existing legal public movement action that closes distance.

The build may change and verify only:

1. `src/validation/vertical_slice_balance_public_policy.gd` to use its existing legal move selection fallback in that condition;
2. the focused Godot regression proving the formerly empty-plan case moves one tile closer;
3. the result-review, Decision addendum, and temporary exact protected-change approval evidence for this one recovery pull request.

## Required invariants

- This is a validation policy recovery, not a combat-value, card-data, resolver, AI-knowledge, Scene, UI, asset, platform, save-schema, or release change.
- The policy may read only the existing public state and must not inspect hidden player plans, hidden technique placement, UI intent, or AI weights.
- Existing reachable-and-affordable public attacks remain higher priority than the fallback movement.
- The fallback uses the existing legality/resource checks and cannot create a new movement rule.
- The full 3,375-scenario report remains deterministic and is not a human balance or usability pass.

## Forbidden scope

- changing candidate, basic-card, martial-manual, route, reward, or stat values;
- changing shared combat resolution, action-selection UI, Scenes, assets, save/load, platform adapters, or release configuration;
- interpreting this fixed-policy matrix as final balance, accessibility, Windows-visible, Android-device, or human-player validation.

## Validation disclosure

- focused Godot RED then GREEN policy regression: required;
- existing instrumentation/privacy/resolver Godot regressions: required;
- two independent full 3,375-scenario headless reports with byte-equality check: required;
- remote PR checks and exact protected-change approval validation: required;
- Windows-visible, human-player, accessibility-user, Android-device, and release-performance validation: `NOT_RUN`.
