# Final layout capture independent review

```yaml
review_mode: READ_ONLY_CAPTURE_AND_SOURCE_CROSSCHECK
reviewer_context: REUSED_AGENT_NOT_FRESH_CONTEXT
product_head: eda26a97f25a931ac02ba739d5c4921720512d41
verdict: ACCEPT_BOUNDED_LAYOUT_CAPTURE_EVIDENCE_WITH_EXPLICIT_LIMITATIONS
new_product_blocker: NONE_FOUND_IN_THIS_REVIEW
whole_blueprint_aesthetic_human_accessibility_device_release_approval: NOT_CLAIMED
reviewer_native_import_full_suite_git_process_operations: NOT_RUN
```

This supplements `layout-final-independent-review.md`; its five recorded full-scope loops are prior evidence, not five new loops performed here. The current pass reviewed images, stored provenance/call records, policy/ratified layout owners, and current three product owners. It does not independently replay runtime, CI, protected checks, or the whole suite. The controller owns ongoing companion documentation and publication; those files were not edited.

## Source identity and retained contract

Fresh-read G AGENTS, local router, current layout Decision/plan, the runtime visual capture policy, previous final independent source review, capture helper/notes and relevant actual board/renderer consumers. Same decision dimension/state as the layout Decision's cited comparison research: no new external rule or aesthetic recommendation is being ratified, and no new benchmarking search was performed for this evidence-only pass.

The three actual source hashes matched before/after image review and the earlier exact-head source review:

| Current source | SHA-256 |
|---|---|
| `src/combat/combat_board_preview.gd` | `791891121D2D13301AF0A0B5E6F50E47951702296FA5BE86D5D2938E942D1A8D` |
| `src/combat/combat_board_preview_auto.gd` | `BF838422E70BB595EBF9E40021DFE253B73136AE2A9A2CAEBA4A843EE96BDC2A` |
| `src/combat/combat_character_placeholder.gd` | `17EEA94F124336617C6046AD1F6FD49084F005ADD64148439EDFD03E18F31FF4` |
| Ignored `layout-capture-probe.gd` | `E3CE523AFF91D7AB689E880C9E7BF888322292A98F6F5333DD9259B0C9771C77` |

Actual crosscheck: auto board 329–378 preserves the state-derived stage and all three background dependencies, applies common floor/composition, uses actual-geometry-only MOVE cancellation and execution-only lanes. Its 456–502 retains distance-derived separation and 46% source-alpha sizing. Base board 1652–1687 validates/clears/refits existing lanes; 1709–1759 preserves both false-return callers and 1.08 ultimate containment. Renderer 104–140 shares draw/bounds math, with actual draw at 323 onward. This is not a domain/AI/save/actor-identity rewrite. The earlier review's low-priority ignored-bool fallback seam at auto 374 remains; these supported viewport captures do not test or close unsupported/missing-node conditions.

No new native or Git comparison was run. Untouched-consumer lineage is the prior exact-head retained-diff review plus unchanged current product hashes and the captured raw product-diff checks, not a freshly claimed full regression suite. In particular, `ObservationRevealPanel` remains its existing independent title/hint layout consumer; screenshot observations below must not silently authorize its modification.

## Artifact and provenance verification

Read-only PowerShell byte/hash/header checks verified **all 33 manifest PNGs 003–035**: canonical SHA-256, byte count, PNG width and height match actual file bytes. Retained source PNGs also match each canonical SHA. All 33 receipt hashes/nonces match manifest, source-absence flags are true, and receipt preparation precedes recorded registration. No images were regenerated, resized, edited or recompressed.

For final **007–035**, inspected all 29 canonical images and independently read all 29 stored call sequences. Every sequence has:

- successful independent `rev-parse HEAD` readback of `eda26a97...`;
- successful captured product-source delta check;
- source-absent preparation before screenshot producer, then registration;
- screenshot return PID equal to recorded exact game PID;
- zero nonzero call exits in that capture's stored sequence;
- receipt/source/evidence commit agreement and original/canonical image hash agreement.

This checks retained evidence, not producer authenticity beyond the policy's `SAME_RUN_PATH_FRESHNESS_NOT_PRODUCER_AUTHENTICITY_OR_VISUAL_QUALITY` ceiling. Editor 28152/game 35992 served 007–032; editor 28152/game 34240 served 033–035. Screenshot scene is the explicitly ignored capture fixture that instantiates the actual Board scene, not the normal run shell.

003 has its valid receipt/source PNG/manifest, but no `layout-003-evidence.json` under the expected local naming scheme; no per-capture call-chain assertion is made for that early historical image. 004/005/006 retain their earlier editor 36628/game 3836 evidence. This absence does not invalidate the independently checked final 007–035 sequence.

| IDs | Actual dimensions | Supported evidence and limits |
|---|---|---|
| 007–011 | 1280×720 | Actual Board/CTA: initial planning, readable current comparison, actual movement checkpoint, visible clash impact, next planning. |
| 012–016 | 1280×800 | Same actual CTA sequence at 800. |
| 017–021 | 1920×1080 | Same actual CTA sequence at 1080. |
| 022 | 1280×800 | Peng peak-trigger checkpoint only. Subsequent failed resize attempt is not repaired by this image. |
| 023–024 | 1280×800 | Actual resolver-seeded Peng event, visible ultimate effect and settled recovery; not ordinary dock/run persistence. |
| 025–026 | 1280×800 | Actual resolver-seeded legacy ultimate and recovery; not evidence of new domain rules. |
| 027–028 | 1280×800 | Muted/reduced-motion Peng impact and recovery. A PNG cannot prove audible silence; no animated peak claim. |
| 029 | 1280×800 | Actual normal player and Dogyeom enemy art at planning. Not Dogyeom player art or an expanded/moving Dogyeom screen. |
| 030 | 1280×800 | Explicit long-copy presentation fixture, not an actual successful combat action. |
| 031–032 | 1280×800 | Actual current comparison to Board skip and next planning; not all physical input devices. |
| 033 | 1280×800 | Fresh Peng peak-trigger checkpoint, followed by the separately baselined resize. |
| 034 | 1920×1080 | Actual native window resize and retained live-effect state, not a fabricated larger canvas. |
| 035 | 1920×1080 | Recovery after 034. `800_PENG_ULTIMATE_RECOVERY` is the originating scenario name, **not** the actual image resolution. |

Viewed historical 004/006 separately: 004 has no readable comparison; 006 has comparison but no visible inline result label. They are not comparison/inline visual PASS. The final later images resolve that capture-gate deficiency without retroactively altering historical evidence.

## Motion/resize evidence precision

Helper 443–452 samples the actual Board tween's `step_finished(0)` scale and requires `(1.08,1.08)` before the peak flag/checkpoint. A callback snapshot and a later PNG are not an atomic frame: do not state that every pixel capture independently proves exactly 1.08. The ordinary frame may finish after the callback. In the successful later resize chain the immediate pre-resize raw baseline is approximately scale `1.078875`, alpha `0.94650131464005`.

Helper 455–496 and 034's retained evidence distinguish that real paused baseline from the peak signal, and require unchanged tween identity/running state, exact alpha/scale and unchanged SFX marker after actual 1920×1080 relayout. This supports bounded preservation; the SFX marker alone is not physical listening or a global no-audio-replay proof. 035 visibly clears the effect/result text after recovery. No manual tween stepping, new motion values or source changes were used by this reviewer.

## Actual visual limitations / actionable follow-up

No new violation of the ratified **bounded** lane/stage contract was established. The screenshots visibly remove the resolved-state lower black void, enlarge actors on the active stage, retain current-action comparison/result versus a separate VFX lane, and restore planning after ordinary/skip flow. Required long result text is present, not clipped away. Nevertheless:

1. **Planning observation hint is visually crowded against ornate frame/seal** (007/012/017 and next-planning images). The fixed `ObservationRevealPanel._layout()` title/hint percentages and clipped labels remain a distinct consumer (`src/ui/observation_reveal_panel.gd:106–124`). Report this as existing presentation debt and inspect a bounded title/hint layout in a separately owned correction; do not call planning typography fully polished.
2. **Long names wrap awkwardly, even when fully contained.** In 030 the final `인` is isolated on its own centered line, and ordinary enemy cost text splits `내력` across lines in 008/013/018. This passes the specified no-truncation geometry boundary but does not establish natural Korean typography/readability. Do not shorten the required fixture copy or shrink fonts to manufacture a stronger claim.
3. **Clash/contact motion strongly superimposes the two actor silhouettes** (010/015/020), and text crosses the silhouette in those frames. The plan explicitly allows actor imagery beneath text lanes; these images are not proof of silhouette distinction or comfortable legibility during actual playback. Preserve current motion/domain behavior in this package and carry that real UX limitation forward.
4. **Planning actors look visually detached from the courtyard surface**, especially 017/021; enlarged recovery 035 also shows a perceptual gap between opaque feet and the drawn shadow. Current nonzero-alpha bounds/shared anchor is the ratified technical measurement, not an opaque-foot/contact perceptual oracle. This observation is not a warrant to change alpha thresholds, approved assets or current motion values. Record it for later visual/contact review rather than asserting grounded aesthetic completion.
5. At 1920×1080 planning, the large lower panel contains substantial unused area; it is the preserved planning composition, not the resolved-state black void this package corrected. Distinguish those two facts in closeout.

The HUD's `방`/`출` labels are actual short source labels (034's state snapshot), not an independently established clipping defect. No false missing-text finding is raised for them.

## Diagnostics and evidence ceiling

Manifest records consistently state **0 errors / 18 warnings**, including one helper warning. The collection helper passes these values as constants. The image/receipt/call sequence is not itself an independent debugger-count query. Direct diagnostic transcript has been requested from the controller; until read, these counts are **controller-recorded**, not reviewer-observed zero-error/warning-free runtime. No clean-warning claim is made.

No whole Blueprint, aesthetic final lock, Human readability/preference, physical input/audio, accessibility-user, Android/device, shipping-rights or release evidence is inferred. No persisted full run was exercised by this fixture: actual CTA is a Board flow, while Peng/legacy events are resolver-seeded and long copy is presentation-only. The source review's automated results remain their own bounded evidence.

Read-only check setup history: one initial PowerShell pipeline form failed to parse; corrected array collection succeeded. A metadata probe tried reserved `$PID` and an unsupported wildcard argument to `rg`; those outputs were discarded and the renamed-variable/exact-directory rerun succeeded for all final 29 records. An expected-name lookup found no historical 003 evidence JSON and was narrowed accordingly. These were reviewer command/setup errors, not product/test failures; no file or process was modified by them.

Only this ignored report was written. No controller companion docs, product/test files, manifests, PNGs, imports, Git state or live game/editor were changed. Publication/protected delivery remains controller-owned.

### Diagnostics follow-up — retained raw response independently compared

After the initial review, the controller supplied the original stored tool response at `.superpowers/sdd/editor-diagnostics-tool-response.json` (SHA-256 `2E3AAE7CB40ECB0A0DF242658B4382B6B1C78EA35FE653A66A3214B024A40F32`) and the reader-facing normalization at `docs/runtime-captures/TEN-COMBAT-LAYOUT-20260909/editor_diagnostics.json` (SHA-256 `F65AEB836D3E8D6DCB207994DFB571031F2183744D17BA12D7AC45CA2B0EFFD3`). I read both fully and independently compared every row's level/path/line/text: **18 rows, 0 mismatches**. The original textual payload and `structuredContent` also match after JSON parsing.

The retained response itself says `source="editor"`, `total_count=returned_count=18`, `offset=0`, `limit=100`, `has_more=false`, `dropped_count=0`, `stale_run_id=false`, `run_id=""`, and `is_running=false`. All 18 rows are `warn`, all originate from `GDScript::reload`, and there are no error-level rows in this complete returned editor buffer. Seventeen concern product scripts (shadowed names, unused arguments, integer/enum or ternary typing warnings); the eighteenth is the ignored capture helper's `owner` shadow warning at line 1012. This is **not** a warning-free result.

The normalization identifies session `combat-layout-20260909@ad2e` and editor 28152, as supplied by the controller's original invocation context. The raw response body does not itself contain that session/PID; it provides no game run identifier. Accordingly the earlier pending-transcript limitation is now resolved for **independent inspection of the retained editor diagnostic response**, and the exact supported wording is “the recorded editor diagnostic buffer contains 0 error rows / 18 warning rows, including one helper warning.” It is not a new live query, a per-capture game-log completeness proof, or proof that every game process was universally error-free. No native/live/process or source operation was performed for this follow-up. The bounded capture verdict and all Human/UX limitations remain unchanged.
