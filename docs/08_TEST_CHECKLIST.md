# 십보강호 테스트 체크리스트

> 책임: 현재 T0 전투 POC의 자동·Godot·Windows·사용자 검증과 T1 진입 게이트  
> 규칙 원본: `docs/02_COMBAT_RULES.md`  
> 범위 원본: `docs/05_COMBAT_POC_SPEC.md`

## 1. 판정 상태

| 상태 | 의미 |
|---|---|
| `PASS` | 명시한 환경·버전·입력에서 실제 결과를 확인 |
| `PARTIAL` | 일부 환경·경로만 확인 |
| `FAIL` | 실제 결과가 기대 결과와 다름 |
| `NOT_RUN` | 실행하지 않음 |
| `BLOCKED` | 도구·권한·선행 구현 부족으로 실행 불가 |

다음을 서로 대체하지 않는다.

1. 파일 존재.
2. 문법·정적 계약.
3. Godot 파싱·headless 실행.
4. Windows 포인터·키보드 조작.
5. 사용자 규칙 이해·재도전 의사.
6. 접근성 실제 검수.
7. 목표 플랫폼 성능 프로파일.
8. Branch protection Required Check 강제.

체크박스는 해당 항목의 실제 증거가 기록됐을 때만 완료한다.

## 2. 현재 증거 요약

### 자동 정적 검사

- [x] Card Component Contract run #414.
- [x] Documentation Governance run #387.
- [x] 카드 데이터 계약.
- [x] 전장·타이밍·HUD·대상·대응·자원 계약.
- [x] `docs/01·02·05`와 실제 데이터의 canonical combat contract.
- [x] 구형 전투 문구와 VCS 충돌 표식 차단.

판정: `PASS`.

### 사용자 Windows 확인

- [x] 전장·배경·HUD·기초 카드 트레이 렌더.
- [x] 1수부터 행동 배치.
- [x] 다중 슬롯 묶음 경계 초과 차단.
- [x] 카드 상세·전투 기록·진행 버튼.
- [x] 이동 목적지 선택.
- [x] 공격 방향 선택.
- [x] 기본 묶음 판정과 다음 묶음 진행.

판정: `PARTIAL` — STEP 0~10·TARGETING 10.5 확인.

### 최신 확인 대기

- [ ] RESPONSE 10.6의 실제 피해 감소·회피·태세 결합.
- [ ] RESOURCE PREVIEW 10.6의 HUD 예상값·해제 복원·부족 잠금.

판정: `NOT_RUN` for latest Windows build.

### 미구현·미실행

- [ ] STEP 11 피격 중단·집중·강건.
- [ ] STEP 12 성향 기반 AI.
- [ ] STEP 13 종료·재시작.
- [ ] STEP 14 POC 플레이테스트.
- [ ] 키보드 전체 입력 흐름.
- [ ] 접근성 사용자 검수.
- [ ] 목표 플랫폼 성능 프로파일.

## 3. 테스트 기록 형식

```yaml
test_id:
build_commit:
godot_version:
platform:
input_method:
precondition:
actions:
expected:
actual:
status: PASS | PARTIAL | FAIL | NOT_RUN | BLOCKED
evidence:
notes:
```

실패는 오류 문장·로그·화면·재현 입력을 함께 남긴다.

## 4. T0 필수 전장 계약

### BOARD-001 기본 구조

- [ ] 전장이 정확히 10칸이다.
- [ ] 타일 번호가 1~10 순서다.
- [ ] 플레이어는 3번, 상대는 8번에서 시작한다.
- [ ] 플레이어와 상대의 스케일이 같다.
- [ ] 양측 발 중심이 타일 앵커에 맞는다.

### BOARD-002 점유·경계

- [ ] 한 칸에 한 전투원만 위치한다.
- [ ] 전장 밖 목적지가 선택 후보에 나타나지 않는다.
- [ ] 상대 점유 칸이 이동 후보에 나타나지 않는다.
- [ ] 조작되지 않은 기본 칸이 포인터 입력을 가로채지 않는다.

### BOARD-003 동시 이동

정상:

- [ ] 서로 다른 유효 목적지면 양측이 동시에 이동한다.

경계:

- [ ] 같은 빈 목적지면 양측 모두 제자리에 남는다.
- [ ] 한쪽 이동만 무효면 유효한 쪽만 정상 처리된다.

금지 회귀:

- [ ] 현재 POC에서 같은 칸 중첩·통과·자리 교환이 발생하지 않는다.

## 5. 라운드·행동 묶음

### TIMING-001 시작

- [ ] 전투가 1라운드 1묶음 1수에서 시작한다.
- [ ] 1~3수 모두 편집 가능하다.
- [ ] 4~10수는 잠금 상태다.

### TIMING-002 묶음

- [ ] 1묶음은 1~3수다.
- [ ] 2묶음은 4~6수다.
- [ ] 3묶음은 7~10수다.
- [ ] 현재 묶음만 편집 가능하다.
- [ ] 판정 완료 묶음은 다시 수정할 수 없다.

### TIMING-003 라운드 진행

- [ ] 1묶음 판정 뒤 4수부터 열린다.
- [ ] 2묶음 판정 뒤 7수부터 열린다.
- [ ] 10수 판정 뒤 다음 라운드 1수로 이동한다.
- [ ] 10수 완료만으로 승패 화면이 열리지 않는다.

### TIMING-004 다중 슬롯

정상:

- [ ] 강공이 연속 두 슬롯을 점유한다.
- [ ] 강공은 마지막 점유 수에서 실행된다.
- [ ] 점유한 어느 슬롯을 눌러도 전체가 해제된다.

실패·경계:

- [ ] 묶음 끝에 한 칸만 남았으면 강공을 배치하지 못한다.
- [ ] 점유 슬롯과 겹치면 배치하지 못한다.
- [ ] 미래 묶음으로 넘어가는 배치를 허용하지 않는다.

## 6. 기초 행동 8종

### ACTION-001 이동

- [ ] 슬롯 1, 기력 0, 내력 0이다.
- [ ] 좌·우 인접 빈 칸만 선택할 수 있다.
- [ ] 선택한 칸으로 이동한다.
- [ ] 비용이 없는 상태에서도 방향 지정은 필요하다.

### ACTION-002 보법

- [ ] 슬롯 1, 내력 1이다.
- [ ] 좌·우 1칸과 2칸을 구분해 선택할 수 있다.
- [ ] 선택한 목적지까지 실제 거리만큼 이동한다.
- [ ] 내력이 부족하면 계획이 무효다.

### ACTION-003 막기

정상:

- [ ] 같은 묶음의 공격은 방어도 4 감소 후보를 가진다.
- [ ] 같은 수 공격은 원피해 50% 감소 후보도 가진다.
- [ ] 두 후보 중 감소량이 큰 하나만 적용한다.
- [ ] 최종 피해는 0 아래로 내려가지 않는다.

반례:

- [ ] 피해 6·같은 수에서 방어도 4가 선택되어 최종 피해 2다.
- [ ] 피해 12·같은 수에서 50% 감소량 6이 선택되어 최종 피해 6이다.
- [ ] 피해 12·다른 수 같은 묶음에서 방어도 4만 적용되어 최종 피해 8이다.

### ACTION-004 회피

- [ ] 같은 수 공격은 최종 피해 0이다.
- [ ] 같은 묶음의 다른 수는 일반 회피로 보호하지 않는다.
- [ ] 방향·사거리 실패 공격과 회피 성공을 같은 원인으로 기록하지 않는다.

### ACTION-005 속공

- [ ] 슬롯 1, 기력 1, 사거리 1, 피해 6이다.
- [ ] 같은 수 이동 전에 실행된다.
- [ ] 선택 방향이 반대면 실패한다.
- [ ] 거리 2 이상이면 실패한다.
- [ ] 비용은 실패해도 반환하지 않는다.

### ACTION-006 강공

- [ ] 슬롯 2, 기력 1·내력 1, 피해 8이다.
- [ ] 지정 방향 거리 1에서 적중한다.
- [ ] 지정 방향 거리 2에서 적중한다.
- [ ] 거리 3 이상에서 실패한다.
- [ ] 반대 방향에서 실패한다.
- [ ] 마지막 점유 수 전에 실행하지 않는다.

### ACTION-007 명상

- [ ] 슬롯 1, 비용 0이다.
- [ ] 실행 시 기력 2·내력 1을 회복한다.
- [ ] 최대치를 넘지 않는다.
- [ ] 실제 실행 순서대로 계획 예상값에 반영된다.

### ACTION-008 태세

- [ ] 슬롯 1, 내력 1이다.
- [ ] 단독 실행 후 다음 유효 공격 피해가 2 증가한다.
- [ ] 공격이 실행된 뒤 보너스가 소모된다.
- [ ] 막기 또는 회피와 결합할 수 있다.
- [ ] 대응 결합 시 단독 공격 보너스를 중복 적용하지 않는다.

## 7. 태세+대응

### RESPONSE-001 입력

- [ ] 태세→막기 순서로 결합한다.
- [ ] 막기→태세 순서로 결합한다.
- [ ] 태세→회피 순서로 결합한다.
- [ ] 회피→태세 순서로 결합한다.
- [ ] 결합 행동은 한 슬롯을 점유한다.
- [ ] 결합 취소 시 예상 자원이 복원된다.

### RESPONSE-002 태세+막기

- [ ] 묶음 전체에 막기를 적용한다.
- [ ] 방어도 4×1.5를 올림해 6으로 사용한다.
- [ ] 같은 수에는 50% 후보와 방어도 6 후보 중 큰 감소량을 사용한다.
- [ ] 결합 비용이 예상 자원과 실제 판정에서 일치한다.

### RESPONSE-003 태세+회피

- [ ] 묶음의 모든 수에서 공격 피해를 0으로 만든다.
- [ ] 방향·사거리 실패와 회피 성공을 로그에서 구분한다.
- [ ] 결합 비용이 예상 자원과 실제 판정에서 일치한다.

밸런스 관찰:

- [ ] 태세+회피가 비용 대비 과도하게 안전한지 기록한다.
- [ ] 적 AI가 해당 상태에 대응할 수단이 필요한지 기록한다.

## 8. 대상·방향 지정

### TARGET-001 이동

- [ ] 이동 후보는 녹색·이동 표식으로 구분된다.
- [ ] 보법 1칸·2칸 후보를 모두 표시한다.
- [ ] 선택 후 슬롯에 방향과 도착 칸 번호가 보인다.
- [ ] 목적지 지정 전 진행 버튼은 비활성이다.

### TARGET-002 공격

- [ ] 속공은 좌·우 거리 1 후보를 표시한다.
- [ ] 강공은 좌·우 거리 1·2 후보를 표시한다.
- [ ] 선택 후 슬롯에 `← 공격` 또는 `→ 공격`이 보인다.
- [ ] 방향 지정 전 진행 버튼은 비활성이다.
- [ ] 적이 반대 방향에 있어도 자동으로 방향을 바꾸지 않는다.

### TARGET-003 예상 위치

- [ ] 앞선 이동을 반영해 이후 이동·공격 후보를 계산한다.
- [ ] 앞선 이동을 해제하면 이후 대상 후보도 다시 검증한다.
- [ ] 실행 시점의 실제 위치가 계획과 달라졌을 때 무효 원인을 기록한다.

## 9. 진행·자원 계획

### GATE-001 진행 조건

- [ ] 현재 묶음의 모든 슬롯이 채워져야 한다.
- [ ] 모든 이동·공격 대상이 지정돼야 한다.
- [ ] 다중 슬롯 점유가 유효해야 한다.
- [ ] 실행 순서 기준 예상 자원이 0 미만이면 안 된다.
- [ ] 조건 충족 시 상태 문구가 `배치 완료`다.
- [ ] 조건 미충족 시 `행동·대상·자원 확인 필요`다.
- [ ] 판정 완료 후 `판정 완료`다.

현재 판정은 동기 처리다. 별도 `resolving` 상태 테스트는 단계별 연출 구현 후 추가한다.

### RESOURCE-001 시작 상태

- [ ] 체력 20/20, 기력 5/5, 내력 4/4로 시작한다.
- [ ] 시작 패널티가 없으면 현재값이 최대값과 같다.
- [ ] `start_penalties`가 해당 자원에서만 감소한다.
- [ ] 시작값이 0 아래로 내려가지 않는다.
- [ ] 절초 기세를 자동 최대치로 만들지 않는다.

### RESOURCE-002 계획 예상값

- [ ] 행동 배치 즉시 기력·내력 예상값이 HUD에 반영된다.
- [ ] 명상 전후 실행 순서가 예상값에 반영된다.
- [ ] 행동 해제 시 처음부터 다시 계산한다.
- [ ] 부족 행동 슬롯에 `자원 부족`이 보인다.
- [ ] 부족 계획은 진행 버튼을 잠근다.
- [ ] 진행 전 실제 전투 자원은 변경되지 않는다.
- [ ] 진행 후 실제 결과와 직전 예상값이 일치한다.

## 10. 판정 순서·동시성

### RESOLVE-001 순서

- [ ] 대응 프로필을 먼저 준비한다.
- [ ] 속공이 이동 전에 실행된다.
- [ ] 이동 후 위치가 일반 공격에 반영된다.
- [ ] 일반 공격 후 명상·단독 태세를 처리한다.

### RESOLVE-002 동시 공격

- [ ] 같은 수·같은 공격 단계 피해를 모두 계산한 뒤 동시에 적용한다.
- [ ] 화면의 표시 순서 때문에 상대 공격을 취소하지 않는다.
- [ ] 현재 POC에서 별도 합 승패·차이 피해가 발생하지 않는다.

### RESOLVE-003 실패 이유

- [ ] 방향 실패.
- [ ] 사거리 실패.
- [ ] 자원 부족.
- [ ] 이동 목적지 충돌.
- [ ] 전장 밖·점유 목적지.
- [ ] 회피.
- [ ] 막기 감소.

각 원인을 서로 다른 로그 문구로 확인한다.

## 11. UI·상호작용

### UI-001 카드·상세

- [ ] 기초 행동 8종이 모두 보인다.
- [ ] `[기초]`, 이름, 기능, 사거리, 슬롯·기력·내력이 보인다.
- [ ] 덱·손패·행동력·공통 막기 경감 정보가 없다.
- [ ] 호버 상세가 열리고 포인터 이탈 시 닫힌다.
- [ ] 클릭 상세가 고정된다.
- [ ] 같은 카드 재클릭 또는 빈 전장 클릭으로 닫힌다.

### UI-002 HUD

- [ ] 상단 순서가 플레이어 상태→플레이어 기세→라운드→상대 기세→상대 상태다.
- [ ] 절초 기세가 정확히 5칸이다.
- [ ] 중앙에 라운드·묶음·선택 수·판정 순서가 보인다.
- [ ] 하단 진행 표기는 `라운드 1`처럼 라운드만 표시한다.
- [ ] 예상 자원과 실제 상태를 구분한다.

### UI-003 기록

- [ ] 기록 패널이 기본적으로 접혀 있다.
- [ ] 포인터로 펼치고 접을 수 있다.
- [ ] 배치·대상·판정·자원 변화가 기록된다.
- [ ] 개발 변수보다 플레이어 원인 문구가 우선한다.
- [ ] 현재 구현에는 로그 자동 스크롤 정책이 없음을 문서·실제 상태가 일치한다.

## 12. STEP 11 예정 검증

다음 항목은 STEP 11 구현 전 `NOT_RUN`이다.

- [ ] 실제 피해를 받으면 이후 미실행 행동이 기본 중단된다.
- [ ] 같은 단계에서 이미 성립한 공격의 동시 피해는 유지된다.
- [ ] `[집중]`은 일반 피해 중단을 방지한다.
- [ ] `[강건]`은 일반 피해 중단을 방지한다.
- [ ] 사망과 강제 제어는 집중·강건보다 우선한다.
- [ ] 다중 슬롯 행동은 전체 점유 슬롯이 취소 표시된다.
- [ ] 취소된 행동과 원인을 UI·로그에서 설명한다.

## 13. STEP 12 AI 예정 검증

- [ ] AI가 플레이어의 현재 계획을 읽지 않는다.
- [ ] 공개 위치·자원·상태·과거 결과만 사용한다.
- [ ] 같은 성향이 반복 가능한 선택 경향으로 나타난다.
- [ ] 완전 무작위가 아니라 상황 판단과 제한된 실수를 사용한다.
- [ ] 같은 시드와 입력에서 선택을 재현한다.
- [ ] AI 계획도 플레이어와 같은 슬롯·대상·자원 검증을 통과한다.

## 14. STEP 13 종료·재시작 예정 검증

- [ ] 체력 0에서 승패를 판정한다.
- [ ] 양측 동시 체력 0의 규칙이 일관된다.
- [ ] 종료 뒤 입력과 추가 판정을 잠근다.
- [ ] 결과 화면에 종료 원인을 표시한다.
- [ ] 재시작 시 전장·라운드·자원·기세·로그·행동 슬롯·대상을 초기화한다.
- [ ] 반복 재시작해도 상태·신호·노드가 누적되지 않는다.

## 15. STEP 14 POC 플레이테스트

핵심 질문:

- [ ] 플레이어가 전장·거리·행동 묶음을 설명하는가?
- [ ] `3수 → 3수 → 4수` 세 묶음의 차이를 느끼는가?
- [ ] 이동 목적지와 공격 방향을 혼동하지 않는가?
- [ ] 실패 이유를 스스로 설명하는가?
- [ ] 상대에게 반복되는 성향을 발견하는가?
- [ ] 대응·위치 선택이 단순 피해 경쟁보다 중요하다고 느끼는가?
- [ ] 한 판 후 다시 시도하고 싶은가?
- [ ] 재시도 때 바꿀 계획을 구체적으로 말하는가?

최소 기록:

```yaml
build_commit:
participant_id:
prior_experience:
completion_time:
input_errors:
rule_questions:
observed_predictions:
failed_predictions:
failure_explanations:
retry_intent:
next_plan:
confusion_points:
```

결과 판정:

```text
KEEP / AMPLIFY / CHANGE / REMOVE / DEFER / RETEST
```

## 16. 접근성 검증

현재 포인터 흐름만 구현돼 있으므로 키보드 항목은 `NOT_RUN`이다.

- [ ] 색 없이 이동·공격·잠금·부족 상태를 구분한다.
- [ ] 키보드만으로 카드→슬롯→대상→진행→상세→기록을 조작한다.
- [ ] 포커스 위치를 항상 확인한다.
- [ ] 긴 한국어 문구가 잘리지 않는다.
- [ ] 최소 해상도에서 전장·진행 버튼·카드가 화면 밖으로 밀리지 않는다.
- [ ] 점멸·흔들림·빠른 모션 감소 경로가 있다.
- [ ] 카드 원화·배경·음향 누락 시 텍스트·기본 도형으로 기능을 이해한다.

실제 접근성 사용자 검수: `NOT_RUN`.

## 17. 성능 검증

- [ ] 목표 Windows 사양을 정의한다.
- [ ] 동일 빌드·동일 설정의 baseline을 기록한다.
- [ ] 대표 장면과 동시 공격·다수 로그의 최악 장면을 분리한다.
- [ ] frame time·CPU·GPU·메모리·로딩 시간을 기록한다.
- [ ] 반복 라운드·재시작 후 노드·메모리 증가를 확인한다.
- [ ] 평균 FPS 하나만으로 통과시키지 않는다.

현재 목표 플랫폼 성능 프로파일: `NOT_RUN`.

## 18. T1 진입 게이트

T0 통과 전에는 다음을 현재 빌드 완료 기준으로 사용하지 않는다.

- 플레이 스타일 2개와 상대 3명.
- 전투 전 정보·제약.
- 전투 후 보상·성장.
- 5전 데모.
- 12세력·10전 전체판.
- 무공·심법·절초·진의.
- 행운·재도전·저장.

T1 진입 조건:

1. STEP 0~13 실제 런타임 통과.
2. 치명적인 입력·판정·복기 오류 없음.
3. STEP 14에서 플레이어가 핵심 규칙과 실패 원인을 설명.
4. 일부 AI 성향 발견.
5. 재도전 의사 또는 구체적 계획 수정 의도 확인.
6. 유지·수정·제거할 T0 규칙을 명시.

## 19. 종료 보고

각 검증 묶음 종료 시 다음을 기록한다.

- 실행한 테스트 ID.
- 빌드·커밋·환경.
- 통과·실패·미실행.
- 새로 발견한 정본·코드·UI drift.
- 수정 뒤 재실행 범위.
- 접근성·성능 상태.
- 사용자 확인 대기.
- 다음 단계 진입 가능 여부.
