# Single execute Blueprint implementation

Spec: `docs/decisions/2026-09-04_THREE_BRANCH_FOUR_CHOICE_JIANGHU_AND_HUMAN_BLUEPRINT_DECISION.md`, section 1.2 and CTA-only supersession. Execute with subagent-driven-development. User's current continuous implementation authorization applies; no further routine approval checkpoint.

## Evidence and feasibility

Source main: `ae2c686cd09fd3c94a309b7fd8104673bf6c0c4d`. Actual consumer `combat_board_preview_auto.gd` still returns after a first plan-lock click. The base class already commits and starts resolution in one call. Thus this is an approved but missing presentation/input behavior, not a new combat rule. FEASIBLE, with duplicate activation and legacy test migration risks. Reuse current assets; no new illustration is needed for this CTA correction.

Reused evidence: `docs/reviews/2026-09-01_FRONTAL_DUEL_REVEAL_AND_CARD_BENCHMARK.md` (10 games) fully reread September 8; same commitment/reveal/hidden-information decision dimension, unchanged combat core. Its mixed/negative full-prediction and deck-copy boundaries still apply. ADAPT separation of planning from playback, ADOPT one meaningful commit action, AVOID a second confirmation surface or hidden-plan exposure. Do not claim fresh player research. Official technical refresh September 8: https://docs.godotengine.org/en/stable/classes/class_basebutton.html — activation-on-release and disabled controls inform native event tests. Phantom Brigade official site refresh returned HTTP 403, so its historical packet is reused with that limitation, not newly verified.

## Global Constraints

The player-facing CTA is exactly `행동 실행`. One valid activation closes planning and starts one resolution; no intermediate player-facing plan-lock click. Preserve private plan commitment, 3/3/4 timings, resource/ultimate reservation semantics, public-only AI, original assets and unrelated dirty work. Reject incomplete/target-incomplete plans and repeated input during resolution. No numerical, reward, save schema, opponent, route, engine-version or audio changes. Standalone review overlay removal is a separate remaining Blueprint gap, not silently completed here. Production and non-shipping scene consumers of the shared progress control must remain coherent. Use failure-first tests and exact-head verification. Never turn a failed test into a blind double-click helper.

## Task 1: Implement the approved single execute transition

Work only in this isolated branch. Fresh-read AGENTS/bootstrap/actual consumer and relevant skills; approved spec above wins over September 1 two-step historical decision. Read all affected inherited methods before editing. Primary files: `src/combat/combat_board_preview_auto.gd`, `src/ui/combat_progress_button.gd`; change base class only if needed for a proven transition invariant. Product data only if its actual label/contract consumer requires alignment. Do not change resolver mechanics.

1. Write a focused failing Godot regression: complete plan, one native pressed/released activation, exactly one resolution starts with lower planning controls closed; no exposed plan-locked-only stop. Exercise incomplete plan, unresolved target, repeated activation and next-bundle readiness. Use genuine state transitions for those assertions; test fixture plan setup is allowed in focused tests, but no terminal injection in campaign.
2. Implement the minimum single transition with label/tooltip/accessibility semantics in agreement. Remove obsolete two-click-only branch behavior where active; preserve internal lock semantics if needed. Avoid alias/dead-state accumulation and unnecessary refactoring.
3. Migrate current tests that intentionally require two clicks to the new approved contract; retain their actual target/resource/reveal/review assertions. Update `tests/probe_native_ten_duel_campaign.gd` to one execute activation and verify exactly-once progress. Do not retain the old 387 activation count as a target; report observed new count. Whole campaign must still prove real HP/history consistency, 10 successful duels, 10 rewards, 36 identified routes, required seven-star/base-ultimate use and exact bidirectional resources.
4. Register the bounded protected-path change through the adopted approval mechanism using this approved Decision/user continuation and exact changed paths. Preserve baseline and archive history, no bypass or imported/UID churn. Follow current project report conventions.
5. Run focused RED then GREEN; related pointer/keyboard/prepare/action/reveal tests; full Python suite once; ordinary-default native full campaign once. Fresh worktree needs headless editor import before runtime. Exact Godot executable: `C:/Users/user/Downloads/Godot_v4.7.1-stable_win64.exe/Godot_v4.7.1-stable_win64_console.exe`. Retain generated import files unstaged for controller tests, do not clean them. Audio exit two-object warning is unresolved known diagnostic, do not suppress it or change audio.
6. Record commands, results, five meaningful full-scope review passes, protected paths, changed native count, remaining Blueprint gaps and NOT_RUN physical/Human/Android/release evidence in `docs/operations/2026-09-08_SINGLE_EXECUTE_BLUEPRINT_EXECUTION_REPORT.md`. Update current authority only to the degree actually demonstrated, preserving historical decisions. Commit scoped work, no push/merge; controller owns independent review and integration.

Controller then performs task review, whole-branch review, independent checks/runtime capture, latest-main reconciliation, exact CI and protected merge/readback. New art/final visual lock is not needed for this textual/control correction.

## Task 2: Avoid identical action-dock reconstruction

The user's same request explicitly includes optimization. After Task 1 review is complete, optimize only `src/ui/action_selection/action_selection_dock.gd` and an adjacent panel/adapter only if measurement proves it necessary. Current consumer creates an adapter and reloads the manual registry even when its inputs are identical; existing panel equality guards prevent node rebuilding only after that work. Controller baseline on unchanged main product: a warmed real dock with four manuals, three samples of 100 identical context updates took 539854 / 563441 / 561096 microseconds. This is a synchronous headless microbenchmark, not FPS or a device claim. Fresh official technical source: https://docs.godotengine.org/en/stable/tutorials/performance/general_optimization.html (September 8); ADOPT measure-before/after, REJECT broad speculative optimization. Same player-visible behavior and commitment/reveal dimension as the reused ten-game packet; no new player design or external gameplay mechanism.

Global constraints from Task 1 remain. Avoid unnecessary reconstruction for identical manual/loadout/mastery inputs while preserving all dynamic updates: resource preview, constraint locks, momentum, reservation, selected source/detail, target and interaction state. Use owned copies for cached comparison so later caller mutation cannot silently stale the UI. Preserve initial setup, clearing/reset/restart and genuinely changed loadout/mastery behavior. Do not introduce a global cache, a frame delay, disk data mutation, schema change, new editor setting or a runtime speed claim without measurement. Do not skip needed readiness initialization just because an empty context compares equal.

1. Add a focused deterministic regression that fails before optimization for redundant reconstruction and proves changed mastery/loadout still rebuilds relevant view models, while resource/constraint/momentum/reservation changes still reach consumers. Reuse existing test style; no hard wall-time threshold in CI. Verify initial empty context and input-copy isolation. Avoid counters added only to satisfy a fake test; measure actual adapter calls or existing node/viewmodel output with focused instrumentation as appropriate.
2. Apply the smallest local invalidation guard. Inspect ultimate panel's own caching so unchanged-context updates do not retain a hidden second registry hot path. Do not refactor the whole adapter or renderer. Record actual changed paths in the existing scoped approval and same-date BUILD record.
3. Rerun the same warmed 100-update measurement (three samples), alongside changed-input correctness assertions. Report both before/after and limits; performance equivalence is not proven by a faster loop alone. Run focused Godot, related dock/constraint/ultimate tests and Python suite. Reuse Task 1 native campaign only if no behavior changes remain; controller may request an exact final native replay for integration.
4. Append the measurements, commands, five scoped full-system review passes and evidence ceilings to the existing single-execute execution report under a separate optimization section. Commit only this task's scoped files. No push/merge/subagents; separate controller task review and final branch review follow.
